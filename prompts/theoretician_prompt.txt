[USER REQUEST]

Subtask:
{subtask}

Path memory summaries:
{memory}

Node metadata:
{node_metadata}

Current node output path:
{path}

Execution notice:
- The path above is the working output directory for this node. Any generated files (plots, data, tables, etc.) must be written to that directory. Do not create any files outside current node output path.
- For any files generated, you should introduce it briefly in your response, including its format, proper way to load it, etc.
- You should not run code in the mode of simulation/mock, make sure you have really executed the code and have given real results.
- Return only the required JSON object (no extra text).
